{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "Place",
    "description": "Entities that have a somewhat fixed, physical extension.",
    "required": [
        "@type",
        "geo"
    ],
    "properties": {
        "@type": {
            "$ref": "@type.schema.json/#@type",
            "enum": [
                "Place"
            ]
        },
        "geo": {
            "$ref": "geo.schema.json/#geo"
        }
    }
}