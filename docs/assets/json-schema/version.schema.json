{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "./version.schema.json",
    "title": "Version of the entity",
    "description": "The version number for the entity. If not versioning is given, consider using semantic versioning (1.0.0) or the publication date",
    "oneOf": [
        {
            "type": "string"
        },
        {
            "type": "number"
        }
    ]
}